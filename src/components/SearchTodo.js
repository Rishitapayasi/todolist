// import React from 'react';

// const SearchTodo = ({ setSearchTerm }) => {
//   const handleSearch = (event) => {
//     setSearchTerm(event.target.value.toLowerCase());
//   };

//   return (
//     <div class="d-flex bg-body-tertiary">
//       <input class="form-control form-control-sm me-2" type="search" placeholder="Search" aria-label="Search"/>
//       <button class="btn btn-outline-primary" type="submit">Search</button>
//     </div>
//   );
// };

// export default SearchTodo;


//  //  const filteredTodos = todos.filter((todo) => (filteredCategory === 'all' ? true : todo.category === filteredCategory))
//  // .filter((todo) => todo.title.toLowerCase().includes(searchTerm));